{
  "name": "@mmd/text-analysis-service",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "uv build",
    "dev": "uv run uvicorn app.main:app --reload",
    "test": "uv run pytest tests/",
    "lint": "uv run flake8 ."
  }
}
