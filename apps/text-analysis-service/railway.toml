[build]
dockerfilePath = "apps/text-analysis-service/Dockerfile"
watchPatterns = [
  "apps/text-analysis-service/**"
]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
