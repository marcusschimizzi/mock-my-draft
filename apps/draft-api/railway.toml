[build]
dockerfilePath = "apps/draft-api/Dockerfile"
watchPatterns = [
  "apps/draft-api/**",
  "package.json",
  "package-lock.json"
]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
